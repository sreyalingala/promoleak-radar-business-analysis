# Diagrams (Assets)

Place exported diagrams here (PNG/SVG) when you have them from architecture or design tools.

**Suggested files (to add later):**

- `context-level1.png` — system context from internal architecture review  
- `promo-state-machine.png` — order discount lifecycle if engineering produces one  
- `dashboard-hifi.png` — once BI/UX creates visuals  

This folder is intentionally light in the first drop so the repo stays easy to navigate.
